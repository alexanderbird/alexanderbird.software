{{#> atoms/page slug='home' default='true' class='pages__home'}}
  {{> atoms/contact-link }}
  <div class='profile'>
    {{> atoms/profile-picture class='profile-picture'}} 
    <h1>
      <span class='name'>{{profile.name}}</span>
      <span class='divider'>&nbsp;&nbsp;//&nbsp;&nbsp;</span>
      <span class='title'>{{profile.title}}</span>
    </h1>
    <h2 class='atoms__tagline'>{{{profile.tagline}}}</h2>
  </div>
  <div class='atoms__row content'>
    {{#each skill-sections }}
      <div>
        {{> organisms/skill-section data=this}}
      </div>
    {{/each}}
  </div>
  <hr>
  <h3 class='atoms__tagline'>Experience</h3>
  <div class='atoms__flex-tiles'>
    {{#each experience }}
      {{> organisms/experience data=this }}
    {{/each}}
  </div>
{{/atoms/page}}
