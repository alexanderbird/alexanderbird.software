<script>
  document.addEventListener('DOMContentLoaded', () => {
    const initializationDelay = 3000
    setTimeout(() => {
      const rootStyle = document.documentElement.style
      rootStyle.removeProperty('--hidden-when-initializing')
      rootStyle.setProperty('--forget-the-lesser-animations', 'none')
      document.querySelector('body').classList.add('javascript-state__fully-initialized')
    }, initializationDelay)
  })
</script>
